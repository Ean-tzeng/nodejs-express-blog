<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div>
      <h1><%= title %></h1>
      <span> <%= sub_title %></span>
      <a href='/chat'>[聊天室]</a>
      <a href='/add'>[新增文章]</a>
    </div>
    <div style='float:right;'>
       <% if(!user){ %>
          <a href='/login'>[登入]</a>
          <a href='/sign_up'>[註冊]</a>        
       <% }else{ %>
          <a href='/logout'>[登出]</a>
       <% } %>
    </div>
   
   
    <% for(index in posts){
          var post = posts[index];
    %>
    <div>
      <h2><a href='/view/<%= post._id %>'>
        <%=
          post.title;
        %>
      </a></h2>
      <div style="float: right;"><%= post.author%>創作於<%= post.createdAt %></div>
    </div>

    <div>
      <%=
        post.content;
      %>
    </div> 
    <%
      }
    %>
    
  </body>
</html>
